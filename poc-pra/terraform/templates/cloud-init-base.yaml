#cloud-config
# Cloud-init de base - Ansible configurera StrongSwan après

package_update: true
package_upgrade: true

packages:
  - python3
  - python3-pip

runcmd:
  - echo "VM ready for Ansible provisioning" >> /var/log/cloud-init-output.log

final_message: "VM is ready for Ansible provisioning"
